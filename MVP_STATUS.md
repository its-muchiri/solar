# MVP Status — Solar (solar.co.ke)

STATE: IN_PROGRESS

## Planning references
- Product spec: ../planning/04-solar-co-ke/prd.md
- Data model: ../planning/04-solar-co-ke/database-schema.md
- API contract: ../planning/04-solar-co-ke/api-endpoints.md
- User flows: ../planning/04-solar-co-ke/user-flows.md
- Open questions (stakeholder-pending decisions): ../planning/04-solar-co-ke/open-questions.md
- Shared modules (auth, payments/escrow, booking engine, KYC, reviews): ../planning/00-portfolio/shared-architecture.md
- Authoritative MVP feature cut & build order: ../planning/00-portfolio/build-sequencing-roadmap.md

## What "MVP complete" means for this project
Per build-sequencing-roadmap.md, solar.co.ke is fourth in the build order. Its MVP is: the system-sizing calculator, installer matching, an e-commerce store for panels/inverters/batteries, and M-Pesa + card payment for high-value transactions — built on shared modules 1–4 plus module 9 (KYC/Verification Pipeline v2, Tier 3 depth). Maintenance subscriptions, installer performance dashboard, extended warranty tracking, and financing are V2/V3 — don't block DONE on them even though a controller for maintenance already exists.

- Customer can sign up / log in
- Customer can run the system-sizing calculator (household/business energy profile → recommended panel/battery/inverter capacity), independent of any installer's quote
- Customer can browse/filter verified installers by certification, rating, and completed-install count
- Customer can request/accept an installation quote, pay a deposit, and pay the remainder on completion/commissioning (milestone payment, not a single lump sum)
- Customer can pay via M-Pesa or card (card matters here given higher transaction values than the other platforms)
- Installer completes KYC Tier 3 onboarding (ID + certification, tracked with expiry) before being listed
- Customer can buy panels/inverters/batteries/accessories from the store, standalone or bundled into a booking
- Customer can submit a review; disputes over mis-sizing/performance route to a technical-review path
- Every image slot (homepage hero, installer directory/profile photos, store product photos for panels/inverters/batteries) shows a real, topically relevant photo sourced from Unsplash — not a placeholder box or broken image
- App builds and runs with zero errors, works on mobile width
- Core flow (size system → pick installer → pay deposit → track → pay remainder → review) covered by a smoke test

## Checklist
Controllers already exist for most of this (src/Controllers/*) — verify against the spec above and the planning docs rather than assuming they're complete, and rather than rebuilding from scratch.

- [x] Identity/auth + installer KYC Tier 3 with certification tracking — this platform had **no auth module at all** (no `src/Core/Auth.php`, no `AuthController`, no bearer-token middleware in `public/index.php`), unlike every sibling platform (laundry/construction/event/rider-co-ke). `$request->user` was always null, so `installation-new.php` shipped with a TODO admitting requests would fail. Built the identical stateless HMAC bearer-token pattern used by the other four platforms: `src/Core/Auth.php` (issue/verify token), `AuthController::signup/login/me`, `Request::bearerToken()`/header collection, and the auth middleware block in `public/index.php` (fails soft to `$request->user = null` if the token or DB is unavailable). Added `/login`, `/signup`, `/onboarding` web routes + views (`login.php`, `signup.php`, `onboarding.php` — the latter posts to the existing `InstallerController::onboard` Tier 3 KYC endpoint with national ID + business registration + certification type/expiry) and `public/assets/js/lib/auth-session.js` for client-side token storage; wired the layout nav to show log in/sign up vs. a logged-in account chip. Added explicit `Response::unauthorized()` guards (matching the sibling-platform pattern) to every write endpoint whose target column is `NOT NULL` and was silently resolving to `null` before: `SizingController::create`, `InstallationController::create/submitQuote/submitSiteSurvey/submitCommissioningReport`, `StoreController::createOrder`, `ReviewController::store`, `DisputeController::store`, `InstallerController::onboard`. Verified the **entire core flow against a real local MySQL database** (previously untested — see Known issues): signup as customer → signup+onboard as installer (KYC doc + EPRA certification with expiry, stored in `installer_certifications`) → admin-equivalent DB flip to `active` → installer appears in `/installers` directory and profile with certification → size a system (authenticated) → book installation → installer submits quote → customer accepts → installer submits site survey → installer submits commissioning report → customer submits review → customer raises a dispute. All 201/200 as expected; unauthenticated attempts on every guarded endpoint correctly return 401 instead of a 500 from the DB. **Not built** (out of scope, matches every sibling platform's established scope discipline): an admin UI to flip installer status from `pending_verification` to `active` — this is done via direct DB/ops action everywhere in this portfolio, not a customer-facing MVP feature.
- [x] System-sizing calculator, independent of installer quotes — `SizingController::calculate()` now implements standard off-grid/hybrid sizing arithmetic (daily load → panel array via Kenya peak-sun-hours; backup duration → battery capacity via depth-of-discharge; connected load → inverter rating with surge headroom), fully documented with its constants/assumptions in that controller's docblock. Built a real appliance add/remove entry UI in `sizing-calculator.js` (previously a TODO stub with a hardcoded empty array) plus optional budget-range inputs. Covered by `tests/sizing_calculator_smoke.php` (6 cases: typical profile, empty appliances, zero backup duration, out-of-range hours, extreme backup duration, zero-watt appliance — all pass). **Assumption taken:** the specific constants (4.5 peak sun hours, 0.78 system derate, 0.8 battery DoD, 1.25 inverter safety margin, KES/kW hardware cost rates) are standard industry-practice defaults, not a stakeholder-confirmed methodology — per open-questions.md #1 this still needs a solar-engineering domain-expert sign-off before the platform treats it as authoritative for dispute resolution (user-flows.md Admin Journey case (b)); the calculator UI now says so explicitly to the customer.
- [x] Installer directory with certification/rating/completed-installs filter — new `src/Models/Installer.php` is the single source for listing rules, filters, sorting and stats; `PageController` (`/`, `/installers`, `/installers/{id}`) and `InstallerController` (new `GET /api/v1/installers`, richer `GET /api/v1/installers/{id}`) both use it. **Listing rule (user-flows.md supply journey step 3):** only `active` installers (KYC approved) with at least one *unexpired* certification appear; a pending installer's profile 404s. Certification standing (`valid`/`expiring_soon` ≤60 days/`expired`) is derived from `expires_at` at read time, because nothing ever updated the stored `status` column. Cards/profile show rating (mean of `reviews.rating` with the installer as reviewee), review count, completed-install count (`solar_bookings.status='completed'`), certification badges, and reviews with first names only. Filter form is plain GET (no JS needed) with empty-result / no-installers-yet states and a 404 for unknown profiles. `InstallerController::onboard` previously *silently skipped* the certification row when type/expiry were missing, letting an installer into the review queue with no tracked certification — it now 422s on a missing/unknown type, a malformed or already-past expiry, and malformed documents. Covered by `tests/installer_directory_smoke.php` (12 cases, transactional fixtures, all passing) and verified over HTTP + headless-Chrome screenshots at 390px and 1200px (no horizontal overflow). Installer cover photos are real, visually-checked Unsplash images (installer lifting a panel onto a roof, hands wiring a panel, two rooftop arrays) served from `src/Core/Photos.php`.
- [ ] Installation booking + milestone payment (deposit at start, remainder at completion/commissioning) (verify InstallationController.php, PaymentController.php)
- [ ] Card payment support alongside M-Pesa for high-value transactions
- [ ] Store: panels/inverters/batteries/accessories (verify StoreController.php against prd.md's E-Commerce Store Scope)
- [ ] Review submission + technical-dispute path for mis-sizing/performance claims (verify ReviewController.php, DisputeController.php)
- [ ] Real Unsplash photography (verified resolving URLs) for hero imagery on home.php/sizing.php and store product photos — **installer-index/installer-profile/home installer cards are done** (see `src/Core/Photos.php`; candidate hero photos already vetted: `1508514177221-188b1cf16e9d`, `1521618755572-156ae0cdd74d`, `1509391366360-2e959784a276`, `1559302504-64aae6ca6b6d` are solar-panel arrays / wiring, but re-check before use) — solar panel/installation subject matter, no placeholders or broken images
- [ ] Error handling (no installers available in area, failed payment, invalid sizing inputs)
- [ ] Smoke test / manual run-through of the full core flow passes
- [ ] Remove stubs, TODOs, placeholder data
- [ ] Cross-check against open-questions.md — where it conflicts with an assumption made here, note the assumption taken and continue (don't stop to ask)

Not MVP per the roadmap — don't block DONE on these even though MaintenanceController.php already exists: maintenance subscription plans, installer performance dashboard, warranty tracking, referral program, financing/installment payments.

## Known issues / open questions
- Mis-sizing liability is prd.md's stated core risk — the sizing calculator is now implemented with its own methodology (not a passthrough to any installer's number), satisfying independence — but the methodology's constants are industry-standard defaults pending a solar-engineering domain-expert review (see open-questions.md #1). Not a blocker for MVP functionality, but flagged for pre-launch legal/technical sign-off.
- `installation_quotes.deviation_flag` is still hardcoded to `false` in `InstallationController::submitQuote()` (see its TODO) — the quote-vs-sizing-calculation deviation check described in user-flows.md step 4 isn't wired up yet, pending open-questions.md #6's threshold decision. Assumption for a future pass: pick a reasonable default threshold (e.g. 15%) rather than leaving this unimplemented indefinitely, and note the assumption when done.
- Financing/installment plans are explicitly out of MVP scope pending a business decision on lending partnerships (see prd.md and open-questions.md) — do not build this.
- A local MySQL instance (`127.0.0.1:3306`, matching `.env`) turned out to be reachable in this pass, superseding the previous pass's note that none was available — used it to run the entire core flow (signup/login → KYC onboarding → sizing → booking → quote → accept → site survey → commissioning → review → dispute) as real HTTP requests against `php -S`, not just unit-style checks. This caught a real bug: `system_sizing_calculations.calculator_version` was `VARCHAR(20)` but `SizingController::CALCULATOR_VERSION` ('v1.0.0-standard-offgrid') is 23 chars, so every sizing save 500'd with a truncation error — invisible in the previous pass because it never reached a real insert. Fixed by widening the column to `VARCHAR(40)` in both `database/schema.sql` and `database/schema.postgres.sql`, and applied the same `ALTER TABLE` to the local dev DB directly (the live Neon/Vercel deployment's schema still needs this migration applied before its first real sizing-calculation write — it hasn't run this ALTER).
- **Assumption (open-questions.md #5):** certification taxonomy is now a fixed 4-value set in `Installer::CERTIFICATION_TYPES` (EPRA solar PV installer licence, EPRA solar PV contractor licence, EPRA electrical installation licence, manufacturer-certified) so the directory can filter on it; the exact licence classes need confirming with EPRA/ops. The onboarding form is a select over this list.
- **Live Neon/Vercel DB needs `database/migrations/2026-09-18_certification_taxonomy_and_sizing_version.sql` applied** (widens `calculator_version`, which supersedes the earlier note above about that ALTER, and maps legacy free-text certification types onto the taxonomy). Local dev DB already has it applied.
- Installer cover photos are picked from a 4-photo pool by `installer id % 4` because there is no installer photo upload/`profile_photo_url` yet (KYC documents are URL references, not uploads). Fine for MVP; real per-installer photos need an upload feature. Consequently two installers can show the same cover.
- `ReviewController::store` trusts a client-supplied `reviewee_id` and doesn't check that the booking is `completed`, that the reviewer is the booking's customer, or that `rating` is 1–5 — the directory's rating average depends on this, so fix it in the review-submission item. `ReviewController::forInstaller` also returns raw `reviews.*` rows.
- The store catalog has only one seeded product (`5kW Hybrid Inverter`) and it has `image_urls = NULL` — the store-photography checklist item below still needs real seed data with Unsplash images for panels/inverters/batteries/accessories, not just a schema/endpoint.

## Changelog
- 2026-09-17: Initial checklist created (assumed generic scope, not sourced from planning/)
- 2026-09-18: Rewritten against planning/04-solar-co-ke and shared-architecture.md; checklist now reflects the roadmap's authoritative MVP cut and points at existing controllers to verify rather than assuming a blank slate
- 2026-09-18: Implemented the system-sizing calculator's actual methodology (SizingController::calculate()), built a real appliance-entry UI, added a standalone smoke test (tests/sizing_calculator_smoke.php, 6/6 passing), and updated README.md/tests/README.md to reflect the calculator is no longer a stub. This was the platform's stated highest-priority feature (core risk mitigation for mis-sizing liability).
- 2026-09-18: Built the entire identity/auth module from scratch (Auth.php, AuthController, bearer-token middleware, Request::bearerToken(), login/signup/onboarding pages, auth-session.js) — this platform was the only one of five sibling platforms with no auth at all, so every write endpoint referencing $request->user was silently broken. Added Response::unauthorized() guards to 8 write endpoints across 6 controllers that insert into NOT NULL user-reference columns. Verified the complete core flow end-to-end against a real local MySQL DB (signup → KYC onboarding with certification tracking → sizing → booking → quote → accept → site survey → commissioning → review → dispute), which surfaced and fixed a real pre-existing bug (calculator_version column too narrow for the stored version string, causing every sizing save to 500) — see Known issues for the still-needed production-DB migration.
- 2026-09-18: Built the installer directory (Installer model, `/installers` filters by certification/rating/completed installs, `GET /api/v1/installers`, profile with reviews + certification standing), tightened installer onboarding validation, added certification taxonomy + migration file, real Unsplash installer cover photos (visually verified), and tests/installer_directory_smoke.php (12/12 passing).
